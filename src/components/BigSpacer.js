import React from "react";
import { View } from "react-native";

const BigSpacer = ({ children }) => {
  return <View style={{ marginBottom: 30, marginTop: 100 }}>{children}</View>;
};

export default BigSpacer;
